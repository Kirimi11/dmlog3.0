import React from "react";
import ReactDOM from "react-dom/client";
import BattleRecord from "./BattleRecord.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BattleRecord />
  </React.StrictMode>
);
